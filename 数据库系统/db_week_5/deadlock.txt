5. 第一个窗口将活期储蓄账户余额更新为1400，更新失败，因为窗口二的事务修改的尚未提交，因为不能修改其他事务修改了但尚未提交的数据，第一个窗口处于等待二个窗口为其解锁的排队状态。

6. 第二个窗口提交后，第一个窗口成功修改。

8. 两个窗口开始一个新事务。

13. 第11步和第12步都造成了当前事务的等待，12步运行后造成了死锁并被postgresql检测到终止事务，因为两个事务互相等待对方解锁其占有的锁。

14. 因为两个事务造成的死锁，postgresql已经终止了这两个事务，因而无法再进行查询。
    只有在结束这个事务代码块后，而且无论使用commit还是rollback，postgresql都将这第一个结束死锁的事务撤销并回滚，然后查询得到的结果就是事务开始前状态。
    而另一个事物则相当于走出了死锁状态，其改变可以继续提交到数据库。
